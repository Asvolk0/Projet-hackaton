{
  "name": "porjet-hackaton",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "node-sass -w assets/scss -o assets/css --output-style=compressed",
    "allCompile": "node-sass -w nosSallesHackaton/assets/scss -o nosSallesHackaton/assets/css --output-style=compressed"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^7.0.1"
  }
}
